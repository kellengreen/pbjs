/*
    [Properties]
        symbol
    [Functions]
        register    (name)      (Controller)
        listen      (path)      (callback)
    [Contollers]
        Base
        Repeat
        If
        ElseIf
        Else
    [Unclassified]
        set
        get
        listen
*/

const pb = function(path) {

};

pb.symbol = Symbol('pb');

pb.watch = path => {

}

pb[pb.symbol] = {

};
