{
  "name": "pbjs",
  "type": "module",
  "version": "0.9.0",
  "license": "ISC",
  "main": "index.js",
  "scripts": {
    "dev": "http-server ./src -c-1 -p 8080 -P http://localhost:8080?",
    "dev:kill": "kill -9 $(pidof 'npm run dev')",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest",
    "format": "prettier --check ./src/",
    "format:fix": "prettier --write ./src/**"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "http-server": "14.x",
    "jest": "30.x",
    "prettier": "3.x",
    "typescript": "^6.0.2"
  }
}
