{
    "name": "pb.js",
  	"version": "0.0.1",
  	"private": true,
    "devDependencies": {
        "gulp": "latest",
        "gulp-watch": "latest",
        "gulp-uglify": "latest",
        "gulp-rename": "latest",
        "gulp-include": "latest"
    },
    "scripts" : { 
        "install" : "npm install -g gulp-cli && npm install -g http-server && npm install -g mocha"
    }
}
